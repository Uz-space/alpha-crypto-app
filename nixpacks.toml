[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/alphacrypto run build"]

[start]
cmd = "npx serve -s artifacts/alphacrypto/dist/public -l $PORT"
