[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/alphacrypto run build"

[deploy]
startCommand = "npx serve -s artifacts/alphacrypto/dist/public -l tcp://0.0.0.0:$PORT"
